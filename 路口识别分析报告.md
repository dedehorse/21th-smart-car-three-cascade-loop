# 智能车左T/右T路口识别逻辑分析报告

## 一、当前系统判断逻辑

### 1.1 路口类型定义（返回值）
```
0 - 直行
1 - 左转弯
2 - 右转弯
3 - 对称T字/十字
4 - 左T字路口 (-|)
5 - 右T字路口 (|-)
```

### 1.2 判断流程图

```
detect_border() 检测边界
    ↓
calculate_center_line() 计算中心线
    ↓
check_turn(0.7f, 0.8f) 判断路口类型
    ↓
├─ 计算左右白像素面积比 (left_ratio, right_ratio)
├─ 检测左右顶点 (left_point, right_point)
├─ 检查分支特征 (has_left_branch, has_right_branch)
└─ 返回路口类型 (0-5)
```

### 1.3 核心判断参数

#### a) 白像素面积比 (left_ratio / right_ratio)
```c
left_ratio = left_white / left_area      // 左半区域白像素占比
right_ratio = right_white / right_area   // 右半区域白像素占比
```
- **含义**：数值越大表示该侧白线越多
- **计算范围**：从 top_point（顶点）到 current_row（中部参考行）
- **判阈值**：threshold1 = 0.7, threshold2 = 0.8

#### b) 左右顶点 (left_point / right_point)
```c
left_point  = min(左边界最小列号)   // 越小越靠左 → 左侧有分支
right_point = max(右边界最大列号)   // 越大越靠右 → 右侧有分支
```
- **计算范围**：从 top_point 到 bottom_point 的所有行
- **阈值**（LCDW=94）：
  - `LEFT_POINT_THRESHOLD = 15`   → left_point < 15 判定为左分支
  - `RIGHT_POINT_THRESHOLD = 78`  → right_point > 78 判定为右分支

#### c) 中心线贯穿度 (top_point)
```c
#define T_JUNCTION_TOP  10   // top_point < 10 → 中心线贯穿 → T字特征
```
- **含义**：越小表示白线在图像顶部越完整，越可能是T字
- **原理**：T字路口的中心线贯穿到顶部，转弯时中心线在中部早已断开

---

## 二、当前实现的优化与改进

### 2.1 已实现的改进（相对第一版）

#### 改进1：引入左右顶点概念
```c
unsigned char left_point  = INVALID_BORDER;   // 新增
unsigned char right_point = 0;                // 新增
```
- **意义**：从仅靠"面积比"进化到"空间位置"
- **优势**：直接反映白线是否延伸到图像边缘，更可靠

#### 改进2：相对阈值替代绝对阈值
```c
has_left_branch  = (ref_left - left_point) >= T_BRANCH_DEVIATION;    // 8像素偏差
has_right_branch = (right_point - ref_right) >= T_BRANCH_DEVIATION;
```
- **意义**：与底部参考边界相比，而非固定阈值
- **优势**：自适应不同赛道宽度和光照条件

#### 改进3：融合多个判据
```
判定左T = (左侧白多 OR 双方都白) AND 
          中心线贯穿(top_point<10) AND 
          左分支延伸(left_point低) AND 
          右侧无分支(!has_right_branch)
```
- **意义**：综合考虑面积、顶点、分支三个维度
- **优势**：大幅降低误判率

---

## 三、存在的风险与混淆点

### 3.1 左T与左转弯的混淆

**危险场景**：
```
左转弯：          |          左T：           -|
        \        |                    \--+--|
         \       |                        |
          \      |                        |
```

**混淆原因**：
- 左转弯也会导致 `left_ratio > right_ratio`
- 左转弯的左边界也可能向外延伸

**当前防护机制**：
```c
if (left_ratio > threshold1 && right_ratio <= threshold1) {
    if (top_point < T_JUNCTION_TOP        // ← 关键防护：T字中心线贯穿，转弯中心线断开
        && has_left_branch                 // ← 左侧确实有分支延伸到边
        && !has_right_branch) {            // ← 右侧没有分支
        return 4;  // 确认是左T
    }
    return 1;  // 否则是左转弯
}
```

**风险评估**：
- ✅ 如果 `top_point >= 10`，会正确退化为左转弯
- ✅ 如果右侧也有分支，不会误判为左T
- ⚠️ **风险点**：如果左转弯路口顶部有断裂，`top_point` 可能 < 10，导致误判

### 3.2 右T与右转弯的混淆

**对称风险**同左T/左转弯。

### 3.3 对称T字与十字的混淆

**危险场景**：
```
对称T(十)：      -|-           十字：      -|-
           \  |  /                     \ | /
            \ | /                       \|/
             \|/                        │
```

**混淆原因**：
- 都是双方都有白像素 (`left_ratio > threshold1 && right_ratio > threshold1`)
- 十字有四个分支，对称T只有上中下

**当前判断逻辑**：
```c
if (top_point < T_JUNCTION_TOP) {
    // 中心线贯穿 → T字路口特征
    if (has_left_branch && has_right_branch) {
        return 3;  // 对称T
    }
} else {
    // top_point >= 10，中心线已断开
    if (left_ratio > threshold2 && right_ratio > threshold2) {
        return 3;  // 十字
    }
}
```

**判别原理**：
- **对称T**：`top_point < 10` (中心线从顶贯穿) + 左右都有分支
- **十字**：`top_point >= 10` (中心线中部断开) + 左右都强 (ratio > 0.8)

**风险评估**：
- ✅ 原理清晰，T字vs十字的核心区分在 `top_point`
- ⚠️ **风险点**：如果十字的中心线恰好贯穿到顶（罕见），可能误判为对称T

### 3.4 边界检测的不对称问题

**问题描述**：
```
赛道宽度不对称 →   left_area ≠ right_area
               →   相同的白线数，比例不同
               →   left_ratio 和 right_ratio 无可比性
```

**示例**：
```
赛道宽度   left_area=30,  right_area=60
同样的T字  left_white=15, right_white=15
结果      left_ratio=0.5, right_ratio=0.25
判定      左侧更强 → 误判方向
```

**当前改进**：引入相对阈值 `T_BRANCH_DEVIATION` 做分支检测，降低对面积比的依赖。

**仍存风险**：如果赛道极度不对称（宽度差异 > 2倍），面积比仍可能误导。

### 3.5 检测窗口脆弱性

**问题**：
```c
int base_search_end = LCDH - 1;  // 搜索到最底部
int search_end = CLAMP(current_row, 0, LCDH / 2);  // 搜索范围有限制
```
- 搜索范围 `[top_point, current_row]` 可能过小
- 在急转弯时，中部参考行 `current_row` 位置不稳定

---

## 四、容易被混淆的场景

### 场景1：视觉透视导致的虚假顶点

```
视角变化：      近处清晰             远处视觉压缩
         上方T字横线清晰    →    横线几乎不可见
         top_point 准确     →    top_point 虚高
```

**后果**：T字在远处可能被识别为转弯。

### 场景2：阴影/光照不均导致的边界抖动

```
阴影区域：
        左边界: 50, 50, 48, 50, 52, 50, 51  →  left_point = 48 (噪声影响)
                ↑ 因阴影抖动
        误判：left_point 可能低于阈值，虽然这里没有分支
```

### 场景3：非标准路口 - "斜T"

```
斜T字（右上左下）：     ╱|
                      ╱ |
                     ╱__|
```

**问题**：
- 上顶点可能不在中心
- left_point 和 right_point 的定义不再对称
- 面积比计算基于垂直中线，无法反映斜角

### 场景4：T字入口与出口混淆

```
正在进入T字：     未进T字：     T字内部：      T字出口：
      │                │           -|-           |
  ----+----            │           | |          -+-
      │                │           | |          | |
```

**当前无法区分**：都会进入 `check_turn` 判定，可能多次识别。

### 场景5："Y"字路口（非标准）

```
Y字：    \  |  /
         \-+-/
           |
```

**结果**：可能被判定为对称T，而实际是三向分支。

---

## 五、阈值选取的风险

### 5.1 硬编码阈值的问题

| 参数 | 值 | 风险 |
|------|-----|------|
| `T_JUNCTION_TOP` | 10 | 不同赛道类型/摄像头标定下，该值应该不同 |
| `LEFT_POINT_THRESHOLD` | 15 | LCDW=160 时改为 25，手动调参容易出错 |
| `RIGHT_POINT_THRESHOLD` | 78 | 依赖 LCDW，计算错误导致混淆 |
| `T_BRANCH_DEVIATION` | 8 | 赛道宽度变化时无法自适应 |
| `threshold1` (check_turn入参) | 0.7 | 光照变化导致白像素面积比波动 |
| `threshold2` (check_turn入参) | 0.8 | 过于接近 0.7，容易越界判定 |

### 5.2 参数初始化的假设

```c
last_left  = LCDW / 4;      // 假设左边界在 1/4 处
last_right = LCDW * 3 / 4;  // 假设右边界在 3/4 处
```

**假设失效场景**：
- 车偏离很远
- 路口极度不对称
- 摄像头标定有偏差

---

## 六、优化建议

### 优先级1：高风险改进

#### 1.1 增强左T/转弯的区分
**当前**：仅用 `top_point` 和 `has_left_branch`
**改进**：加入水平分支验证
```c
// 检查左侧是否存在明显的水平白线（分支）
int horizontal_branch_score = scan_horizontal_white_near_edges(left_point);
if (top_point < T_JUNCTION_TOP && has_left_branch 
    && horizontal_branch_score > THRESHOLD) {  // ← 新增
    return 4;  // 左T
}
```

#### 1.2 自适应阈值系统
```c
// 根据赛道宽度自适应调整
int track_width = ref_right - ref_left;
int adaptive_T_BRANCH_DEVIATION = (track_width * 8) / 100;  // 宽度的8%
```

#### 1.3 防止视觉抖动
```c
// 采用滑动平均或卡尔曼滤波
static unsigned char last_left_point = INVALID_BORDER;
if (abs(left_point - last_left_point) > 10) {
    // 超出合理范围，采用上一帧值或平均值
    left_point = (left_point + last_left_point) / 2;
}
last_left_point = left_point;
```

### 优先级2：中等改进

#### 2.1 路口分类统计
```c
// 记录最近10帧的判定结果，采用投票机制
static unsigned char turn_history[10];
turn_history[frame_counter % 10] = check_turn(...);
unsigned char final_turn = majority_vote(turn_history);
```

#### 2.2 增加参考框架的鲁棒性
```c
// 当前仅用底部5行，改为分析中部稳定段
for (int row = LCDH * 2/5; row < LCDH * 3/5; row++) {
    if (left_border[row] != INVALID_BORDER) {
        // 采样中部参考
    }
}
```

#### 2.3 十字vs对称T的额外验证
```c
// 十字应该在中部有横向白线
int mid_row = LCDH / 2;
int mid_white_count = count_white_pixels_in_region(mid_row, ...);
if (top_point >= T_JUNCTION_TOP && mid_white_count > THRESHOLD) {
    return 3;  // 十字确认
}
```

### 优先级3：后续优化

#### 3.1 方向一致性检查
```c
// T字转弯后，方向应该一致
if (last_turn == 4 && current_turn == 1) {  // 左T后左转
    confidence_score += 20%;  // 提高置信度
}
```

#### 3.2 多模态传感器融合（如有IMU）
```c
// 结合转向角速度验证路口类型
if (gyro_z > THRESHOLD && turn_flag == 4) {  // 左转+陀螺仪左转
    // 双重确认
}
```

---

## 七、调试与验证方法

### 7.1 显示关键变量
```c
// 在 DisplayTrack() 中添加
sprintf(txt, "turn:%d top:%d left_pt:%d right_pt:%d", 
        turn_flag, top_point, left_point, right_point);
Display_showString(0, 0, txt, U16_WHITE, U16_BLACK, 12);

sprintf(txt, "left_ratio:%.2f right_ratio:%.2f",
        left_ratio, right_ratio);
Display_showString(0, 1, txt, U16_WHITE, U16_BLACK, 12);
```

### 7.2 日志记录
```c
if (turn_flag != last_turn) {  // 路口类型变化时
    printf("TURN_CHANGE: %d→%d (top=%d, left_pt=%d, right_pt=%d, "
           "left_ratio=%.2f, right_ratio=%.2f)\n",
           last_turn, turn_flag, top_point, left_point, right_point,
           left_ratio, right_ratio);
}
```

### 7.3 实测场景列表
- [ ] 直路
- [ ] 左转弯（缓/急）
- [ ] 右转弯（缓/急）
- [ ] 左T路口
- [ ] 右T路口
- [ ] 十字路口
- [ ] 对称T字
- [ ] 特殊场景（阴影、光线不足、高反差）

---

## 八、总结

### 当前系统优点
1. ✅ 已从面积比单一判据进化为"面积比 + 顶点位置 + 分支验证"的三维判定
2. ✅ 引入相对阈值，提高自适应能力
3. ✅ 逻辑清晰，维护性好

### 主要风险
1. ⚠️ 左T/转弯、右T/转弯、对称T/十字的边界判定脆弱
2. ⚠️ 硬编码阈值对不同赛道/标定不适应
3. ⚠️ 视觉噪声（阴影、光照）可能导致顶点抖动
4. ⚠️ 无法处理斜T、Y字等非标准路口

### 建议优先行动
1. **立即**：增加水平分支验证，区分T字与转弯
2. **短期**：实现自适应阈值和滤波，抗噪声
3. **中期**：多帧投票机制，提高稳定性
4. **长期**：基于深度学习的路口分类替代规则引擎
